---
import Layout from '@layouts/Layout.astro';
import { getStaticPathsWrapper } from '@util/getStaticPaths';
import Career from '@components/career/index.astro';

export const getStaticPaths = getStaticPathsWrapper(['pak', 'are'], {
  pak: ['en', 'ur'],
  are: ['en', 'ar'],
});
---

<Layout>
  <Career />
</Layout>
