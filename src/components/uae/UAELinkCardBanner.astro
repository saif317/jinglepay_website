---
/**
 * UAE Link Card & Add Fund Banner
 *
 * Second banner section for UAE landing page. Background lifestyle image on the left
 * with overlay UI elements (bank tile on left, headline and add-fund card on right).
 * Semantic, responsive, accessible HTML without copy-pasting Figma export.
 */
import { Image } from 'astro:assets';

import bannerBg from '../../assets/uae-banners/banner2/0846311bdd1a736419e12649e36d3b01157d0198.jpg';
// TODO: Replace placeholders when real assets provided

---

<section class="relative w-full overflow-hidden h-[650px] md:h-[750px] lg:h-[850px]">
  <!-- Background -->
  <Image src={bannerBg} alt="Customer linking card on mobile" class="absolute inset-0 object-cover w-full h-full z-0" loading="eager" />

  <!-- Radial light overlay for subtle focus -->
  <div class="absolute inset-0 z-10 bg-[radial-gradient(circle_at_center,rgba(255,255,255,0.15)_0%,rgba(255,255,255,0)_60%)]"></div>

  <!-- Content Wrapper -->
  <div class="relative z-20 flex items-center justify-between px-6 md:px-12 h-full">
    <!-- Bank Tile -->
    <div
      class="hidden md:flex flex-col gap-1 bg-white rounded-xl shadow-md px-6 py-4 max-w-xs w-full"
      aria-label="Linked bank: First Abu Dhabi Bank (1 account)">
      <div class="flex items-center gap-4">
        <div class="shrink-0 w-12 h-8 bg-blue-600 rounded-sm" aria-label="FAB bank logo placeholder"></div>
        <div>
          <p class="text-sm font-bold text-gray-900">First Abu Dhabi Bank</p>
          <p class="text-xs font-medium text-gray-700">1 account</p>
        </div>
      </div>
    </div>

    <!-- Right column (headline + add-fund card) -->
    <div class="flex flex-col items-start gap-6 max-w-md w-full ml-auto">
      <h2 class="text-3xl md:text-5xl font-extrabold leading-tight text-white/80">
        Link your card &amp; add fund
      </h2>

      <!-- Add fund card -->
      <div class="bg-white rounded-2xl shadow-lg p-6 w-full max-w-md" role="group" aria-labelledby="add-fund-heading">
        <p id="add-fund-heading" class="mb-4 text-sm text-gray-700">Enter an amount greater than AED&nbsp;50</p>

        <!-- Amount input (static display style) -->
        <div class="flex items-center justify-between border rounded-lg px-4 py-3 mb-4">
          <span class="text-xl text-gray-800" aria-label="Amount">1,000</span>
          <div class="flex items-center gap-1" aria-label="Currency AED">
            <span class="text-2xl">🇦🇪</span>
            <span class="text-lg text-gray-800 font-medium">AED</span>
          </div>
        </div>
        <div class="flex items-center justify-between text-xs text-gray-700">
          <span>Current Balance</span>
          <span>0.00 AED</span>
        </div>
      </div>
    </div>
  </div>
</section>
