---
// Props for customization (if needed)
interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
---

<button
  class={`cursor-pointer qr-modal-trigger flex justify-center items-center ${className}`}
  onclick='window.openQRModal()'
>
  <slot />
</button>
