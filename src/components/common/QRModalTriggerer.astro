---
// Props for customization (if needed)
interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
---

<button class={`cursor-pointer qr-modal-trigger ${className}`} onclick='window.openQRModal()'>
  <slot />
</button>
