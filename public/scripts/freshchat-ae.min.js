function initFreshChat() {
  window.fcWidget.init({
    token: '3bfbc8d6-efcf-4b11-80d2-fdcadb0255c3',
    host: 'https://esd1-350615778585313396.freshchat.com',
    config: {
      headerProperty: {
        backgroundColor: '#7e5cef', // Purple header color from image
        foregroundColor: '#ffffff', // White text color
      },
      content: {
        headers: {
          chat: 'Message Us',
        },
      },
      styles: {
        launcher: {
          // Your chat button
          'background-color': '#7A2EFF', // Your exact purple
          // Add other styles like width, height, border-radius if needed
        },
        header: {
          // Already covered by headerProperty, but you *could* add more here
          // 'border-bottom': 'none', // Example
        },
        input: {
          // The text input field
          border: 'none', // Remove default border if any
          // 'background-color': '#FFFFFF', // Ensure white background
          // Add padding/margin if needed
        },
        userBubble: {
          // Your messages
          'background-color': '#F2E8FF', // Your light lavender
          color: '#7A2EFF', // Your dark purple text
          'border-radius': '18px 18px 5px 18px', // Your specific rounding
        },
        agentBubble: {
          // Jingle Pay's messages
          'background-color': '#F5F5F5', // Your light gray
          color: '#333333', // Your dark text
          'border-radius': '18px 18px 18px 5px', // Your specific rounding
          'box-shadow': 'none', // Remove default shadow if desired
        },
        sendButton: {
          // The send icon button
          'background-color': '#7A2EFF', // Your exact purple
          color: '#FFFFFF', // Ensure icon inside is white (if controlled by 'color')
          'border-radius': '50%', // Make it circular
          // Adjust width/height if needed via documentation or experimentation
        },
      },
    },
  });
}

function initialize(i, t) {
  var e;
  i.getElementById(t)
    ? initFreshChat()
    : (((e = i.createElement('script')).id = t),
      (e.async = !0),
      (e.src = 'https://esd1-350615778585313396.freshchat.com/js/widget.js'),
      (e.onload = initFreshChat),
      i.head.appendChild(e));
}

function initiateCall() {
  initialize(document, 'Freshchat-js-sdk');
}

window.addEventListener
  ? window.addEventListener('load', initiateCall, !1)
  : window.attachEvent('load', initiateCall, !1);
